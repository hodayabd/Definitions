import React from "react";
import ReactDOM from "react-dom";
import "./header.css";

const Header = () => (
  <div className="Header">
    <h1> react Shop</h1>
  </div>
);

export default Header;